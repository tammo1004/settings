PHP >= 7.1.3
OpenSSL PHP Extension
PDO PHP Extension
Mbstring PHP Extension
Tokenizer PHP Extension
XML PHP Extension
Ctype PHP Extension
JSON PHP Extension
BCMath PHP Extension

#php.ini
extension=fileinfo
extension=mbstring
extension=openssl

>php --version
>php -m


Composer: https://getcomposer.org/download/
Composer-Setup.exe

>composer --version
Composer version 1.8.0 2018-12-03 10:31:16


Type1:
>cd Apache24
>composer update
>composer global require laravel/installer
>laravel --version
Laravel Installer 2.0.1
>laravel new laravel_home

Type2:
>cd Apache24>
>composer create-project --prefer-dist laravel/laravel laravel_home


>cd laravel_home
>php artisan --version
>php artisan serve
Laravel development server started: <http://127.0.0.1:8000>


>vi Apache24/conf/httpd.conf
#DocumentRoot "${DOCROOT}/htdocs"
#<Directory "${DOCROOT}/htdocs">
DocumentRoot "${DOCROOT}/www/public"
<Directory "${DOCROOT}/www/public">

mysql> ALTER USER 'root' IDENTIFIED WITH mysql_native_password BY '*****';
mysql> exit
>brew services restart mysql

mysql> CREATE DATABASE test;
mysql> use test;
mysql> FLUSH PRIVILEGES;
mysql> CREATE TABLE messenger (
idx INT(11) UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,	
title VARCHAR(255),
in_date DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci;
mysql> INSERT INTO messenger (title) VALUES('Hello Laravel!!!');
mysql> exit


# config/database.php
            'database' => env('DB_DATABASE', ''),
            'username' => env('DB_USERNAME', ''),
            'password' => env('DB_PASSWORD', ''),

>php artisan tinker
